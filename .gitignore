node_modules
dist
.env.local
.env
*.log
.DS_Store

# throwaway dev artifacts (screenshots, state dumps)
.shots/
.ha_states.json

# LLM prototype key (never commit)
.anthropic-key

# local HA token for rotation (never commit)
.ha_token

# local dev/screenshot artifacts
.devport
.devserver.log
.shots/

# Brewfather API creds for local testing (never commit)
.bf-creds
